import type { Readable } from "svelte/store";
declare type Props = {
    onConnect?: () => void;
    onDisconnect?: () => void;
};
declare const useConnect: (props?: Props) => {
    principal: Readable<string | undefined>;
    status: Readable<any>;
    activeProvider: Readable<any>;
    isInitializing: Readable<boolean>;
    isConnected: Readable<boolean>;
    isConnecting: Readable<boolean>;
    isDisconnecting: Readable<boolean>;
    isIdle: Readable<boolean>;
    connect: (provider: any) => void;
    disconnect: () => void;
};
export { useConnect };
